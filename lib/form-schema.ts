import {z} from 'zod';
export const leadSchema=z.object({name:z.string().trim().min(2,'Вкажіть імʼя, щонайменше 2 символи.').max(80,'Імʼя має бути до 80 символів.'),contact:z.string().trim().max(100).refine(v=>/^@[A-Za-z0-9_]{5,32}$/.test(v)||/^\+?[\d\s()-]{9,22}$/.test(v)&&v.replace(/\D/g,'').length>=9,'Вкажіть телефон із кодом країни або Telegram у форматі @username.'),business:z.string().trim().max(500,'Скоротіть опис до 500 символів.').optional(),volume:z.string().max(50).optional(),consent:z.boolean().refine(Boolean,'Підтвердьте згоду на звʼязок щодо заявки.'),website:z.string().max(200).optional(),formId:z.enum(['demo','partner','contact']),plan:z.enum(['demo','start','business','scale','']).optional(),market:z.enum(['ua','eu','']).optional()});
export type LeadInput=z.infer<typeof leadSchema>;
