import type {MetadataRoute} from 'next';
import {articles} from '@/lib/blog';
export default function sitemap():MetadataRoute.Sitemap{const pages=['','/ai-agent-dlya-prodazhiv','/tarify','/mozhlyvosti','/integratsii','/integratsii/telegram','/integratsii/instagram','/integratsii/whatsapp','/integratsii/site-widget','/integratsii/calendar','/integratsii/crm','/rishennia/ecommerce','/rishennia/poslugy','/rishennia/b2b','/keisy','/bezpeka','/resursy','/demo','/kontakty','/blog'];return [...pages.map(path=>({url:`https://www.ministrysale.org${path}`,changeFrequency:'monthly' as const,priority:path?path.startsWith('/rishennia')?0.8:0.7:1})),...articles.map(article=>({url:`https://www.ministrysale.org/blog/${article.slug}`,lastModified:new Date(article.publishedAt),changeFrequency:'monthly' as const,priority:0.8}))]}
