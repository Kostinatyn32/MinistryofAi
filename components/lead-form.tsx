'use client';
import {useForm} from 'react-hook-form';
import {zodResolver} from '@hookform/resolvers/zod';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {LoaderCircle,Send,ShieldCheck} from 'lucide-react';
import {leadSchema,type LeadInput} from '@/lib/form-schema';
import {Button} from '@/components/ui/button';
import {track} from '@/lib/analytics';
export function LeadForm({formId='demo',plan='',market='',ready=false}:{formId?:LeadInput['formId'];plan?:LeadInput['plan'];market?:LeadInput['market'];ready?:boolean}){const router=useRouter();const [failure,setFailure]=useState('');const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm<LeadInput>({resolver:zodResolver(leadSchema),defaultValues:{name:'',contact:'',business:'',volume:'',website:'',consent:false,formId,plan,market}});async function submit(data:LeadInput){setFailure('');if(!ready){const subject=encodeURIComponent('Заявка на пілот AI-продавця');const body=encodeURIComponent(`Імʼя: ${data.name}\nКонтакт: ${data.contact}\nБізнес: ${data.business||'—'}\nЗвернень на місяць: ${data.volume||'—'}\nПлан: ${data.plan||'demo'}\nРинок: ${data.market||'—'}`);track('demo_form_started',{form_id:formId,delivery:'email_fallback'});window.location.href=`mailto:info@ministrysale.org?subject=${subject}&body=${body}`;return}try{const response=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!response.ok)throw new Error(response.status===429?'Забагато спроб. Спробуйте за 10 хвилин.':'Не вдалося надіслати заявку.');track('demo_form_submitted',{form_id:formId});router.push('/dyakuyemo')}catch(error){track('demo_form_error',{form_id:formId});setFailure(error instanceof Error?error.message:'Не вдалося надіслати заявку.')}}return <form onSubmit={handleSubmit(submit)} className="lead-form" noValidate><h2>{formId==='partner'?'Обговорімо партнерство':'Податися на пілот'}</h2><p>Залиште контакт, за яким вам зручно спілкуватись.</p>{!ready&&<div className="form-notice">Поки налаштовуємо автоматичну доставку заявок. Після натискання відкриється лист із заповненими даними — його потрібно надіслати. Або напишіть у <a href="https://t.me/tripailo_ads">Telegram</a>.</div>}<label htmlFor={`${formId}-name`}>Ваше імʼя</label><input id={`${formId}-name`} autoComplete="given-name" placeholder="Як до вас звертатись?" {...register('name')} aria-invalid={!!errors.name} aria-describedby={errors.name?`${formId}-name-error`:undefined}/>{errors.name&&<span className="field-error" id={`${formId}-name-error`}>{errors.name.message}</span>}<label htmlFor={`${formId}-contact`}>Телефон або Telegram</label><input id={`${formId}-contact`} autoComplete="tel" placeholder="+380 або @username" {...register('contact')} aria-invalid={!!errors.contact} aria-describedby={errors.name?`${formId}-contact-error`:undefined}/>{errors.contact&&<span className="field-error" id={`${formId}-contact-error`}>{errors.contact.message}</span>}<label htmlFor={`${formId}-business`}>Сфера бізнесу <span>необовʼязково</span></label><textarea id={`${formId}-business`} placeholder="Що продаєте та які канали використовуєте?" rows={3} {...register('business')} aria-invalid={!!errors.business}/>{errors.business&&<span className="field-error">{errors.business.message}</span>}{formId==='demo'&&<><label htmlFor={`${formId}-volume`}>Звернень на місяць <span>якщо знаєте</span></label><input id={`${formId}-volume`} placeholder="Ваша приблизна оцінка" {...register('volume')}/></>}<div className="honeypot" aria-hidden="true"><label htmlFor={`${formId}-website`}>Ваш сайт</label><input id={`${formId}-website`} tabIndex={-1} autoComplete="off" {...register('website')}/></div><label className="consent-check"><input type="checkbox" {...register('consent')}/> <span>Погоджуюсь на звʼязок щодо цієї заявки. <a href="/privacy-policy">Обробка персональних даних</a>.</span></label>{errors.consent&&<span className="field-error">{errors.consent.message}</span>}{failure&&<div role="alert" className="form-notice">{failure} Напишіть у <a href="https://t.me/tripailo_ads">Telegram @tripailo_ads</a> або на <a href="mailto:info@ministrysale.org">info@ministrysale.org</a>.</div>}<Button type="submit" disabled={isSubmitting}>{isSubmitting?<LoaderCircle size={18}/>:<Send size={17}/>} {isSubmitting?'Надсилаємо…':ready?(formId==='partner'?'Надіслати заявку':'Податися на пілот'):'Відкрити лист для заявки'}</Button><div className="form-micro"><ShieldCheck size={16}/> Відповідаємо протягом робочого дня. У розсилку ви не потрапляєте.</div></form>}
